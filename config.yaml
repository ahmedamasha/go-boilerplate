server:
  port: "8080"
  host: "0.0.0.0"

database:
  host: "localhost"
  port: "5432"
  user: "refda"
  password: "refda"
  dbname: "refda"
  sslmode: "disable"

jwt:
  secret: "change-me-in-production-refda-jwt-secret"
  expiry_hours: 168

otp:
  expiry_minutes: 5
  length: 4
  mock_fixed_code: "1234"  # dev only — always use this code instead of random

storage:
  type: "local"
  local_path: "./uploads"
  base_url: "http://localhost:8080/uploads"

payment:
  mock: true

admin:
  api_key: "12345"
